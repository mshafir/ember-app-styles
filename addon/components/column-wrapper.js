import Component from '@ember/component';
import layout from '../templates/components/column-wrapper';

export default Component.extend({
  layout,
  classNames: ['column', 'column-wrapper', 'flex']
});
